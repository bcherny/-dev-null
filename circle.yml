dependencies:
  pre:
    - npm install -g gulp nodemon mocha
    - cd server; npm i
    - cd www; npm i

test:
  pre:
    - mysql -u ubuntu circle_test < server/tests/prefill.sql

  override:
    - cd server; npm test